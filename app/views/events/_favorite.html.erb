<div>
  <% if current_user.favorite_by?(event.id) %>
    <%= link_to 'お気に入りから外す', destroy_favorite_path(event.id), method: :delete %>
  <% else %>
    <%= link_to 'お気に入り登録する', create_favorite_path(event.id), method: :post %>
  <% end %>
</div>
